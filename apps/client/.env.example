# Leave empty to call /api on the same origin (Vite proxies to the Nest API).
# Set to an absolute API origin only when the UI is not served next to the API.
# VITE_API_BASE_URL=http://localhost:4000
